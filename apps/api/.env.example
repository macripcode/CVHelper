PORT=3001
WEB_ORIGIN=http://localhost:5173

# Optional. Only needed for the "Read" button's automatic company-name detection
# (POST /api/company/extract) — the only part of CVHelper that calls an AI model.
GEMINI_API_KEY=
